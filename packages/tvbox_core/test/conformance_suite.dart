/// conformance 测试套件已迁至 `lib/conformance_suite.dart`（可跨包复用）。
///
/// 本文件保留为占位，避免旧引用失效：
/// ```dart
/// import 'package:tvbox_core/conformance_suite.dart';
/// void main() => runJsConformanceSuite(myFactory);
/// ```
library;

// 无测试用例：套件由各引擎实现的测试文件按需挂载。
